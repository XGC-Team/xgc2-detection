name: ci

# Parked imports live under temp/ and are not being integrated.

on:
  workflow_dispatch:

jobs:
  aggregator-ci-disabled:
    name: Aggregator CI disabled
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "xgc2-detection is not integrating packages under temp/."
